const isValidImageUrl = (url) => { if (!url || typeof url !== 'string') return false; try { const parsed = new URL(url); return ['http:', 'https:', 'data:'].includes(parsed.protocol); } catch { return false; } }; const htmlEsc = (s) => { return String(s ?? '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;').replace(/'/g, '&#39;'); }; const xmlEsc = (s) => { return String(s || '').replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;'); }; const assertDependencies = () => { if (typeof JOURNALS === 'undefined') { throw new Error('JOURNALS tidak di-load dari data.js'); } if (typeof TEMPLATE_SCHEMA === 'undefined') { throw new Error('TEMPLATE_SCHEMA tidak di-load dari template-schema.js'); } if (typeof IDENTITY === 'undefined') { throw new Error('IDENTITY tidak di-load dari data.js'); } }; const getSchemaSection = (schemaArray, minRequired = 6) => { if (!Array.isArray(schemaArray) || schemaArray.length < minRequired) { throw new Error(`Schema array harus punya minimal ${minRequired} sections`); } return schemaArray; }; const validateJournal = (journal) => { const errors = []; if (!['daily', 'weekly'].includes(journal.type)) { errors.push('Type harus daily atau weekly'); } if (!/^\d{4}-\d{2}-\d{2}$/.test(journal.dateSort)) { errors.push('dateSort format invalid (YYYY-MM-DD)'); } if (journal.daily?.activities) { journal.daily.activities.forEach((act, idx) => { if (!act.time || !act.activity) { errors.push(`Activity[${idx}] missing required fields`); } }); } if (journal.weekly?.activities) { journal.weekly.activities.forEach((act, idx) => { if (!act.day || !act.focus) { errors.push(`Weekly Activity[${idx}] missing required fields`); } }); } return { valid: errors.length === 0, errors }; }; const showErrorBanner = (message) => { const banner = document.createElement('div'); banner.setAttribute('role', 'alert'); banner.style.cssText = 'position:fixed;top:0;left:0;right:0;z-index:9999;background:#fee;border-bottom:2px solid #c00;padding:15px 20px;display:flex;align-items:center;gap:12px;font-family:Arial,sans-serif;font-size:14px;color:#900;box-shadow:0 2px 8px rgba(0,0,0,0.1);'; const icon = document.createElement('span'); icon.textContent = '⚠️'; icon.style.cssText = 'font-size:18px;flex-shrink:0;'; const textBlock = document.createElement('div'); textBlock.style.flex = '1'; textBlock.textContent = message; const closeBtn = document.createElement('button'); closeBtn.textContent = '✕'; closeBtn.setAttribute('aria-label', 'Tutup peringatan'); closeBtn.style.cssText = 'background:none;border:none;cursor:pointer;font-size:16px;color:#900;flex-shrink:0;padding:0 4px;'; closeBtn.addEventListener('click', () => banner.remove()); banner.appendChild(icon); banner.appendChild(textBlock); banner.appendChild(closeBtn); document.body.prepend(banner); return banner; }; const handleError = (context, error) => { console.error(`Error in ${context}:`, error); const message = error.message || 'Terjadi kesalahan. Silakan coba lagi.'; showErrorBanner(message); };